{
  "extends": [
    "react-app"
  ],
  "rules": {
    "semi": ["error", "always"],
    "keyword-spacing": ["error", { "before": true }],
    "space-before-blocks": "error"
  },
  "overrides": [
    {
      "files": ["**/*.ts?(x)"],
      "rules": {}
    }
  ]
}
